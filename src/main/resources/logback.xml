<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.desktopai/logs/desktopai.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.desktopai/logs/desktopai.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.desktopai" level="DEBUG"/>

    <!-- Suppress noisy third-party loggers -->
    <logger name="org.apache.tika" level="WARN"/>
    <logger name="ai.djl" level="WARN"/>
    <logger name="ai.onnxruntime" level="WARN"/>
    <!-- llama.cpp native logs routed via LocalNativeProvider.installLoggerHook() -->
    <logger name="llama.cpp" level="WARN"/>
    <!-- Suppress JavaFX unnamed-module warning -->
    <logger name="com.sun.javafx" level="ERROR"/>
    <logger name="javafx" level="ERROR"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
